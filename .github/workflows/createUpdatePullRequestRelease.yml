name: Create or Update Pull Request Release

on:
  push:
    branches: [staging, main]

jobs:
  check:
    name: Create or update pull request release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.2.0
        with:
          fetch-depth: 0 # Required due to the weg Git works, without it this action won't be able to find any or the correct tags
      - uses: actions/setup-node@v2
        with:
          node-version: 14
      - uses: borales/actions-yarn@v2.3.0
        with:
          cmd: add semver
      - name: 'Get Previous tag'
        id: previoustag
        uses: "WyriHaximus/github-action-get-previous-tag@v1"
      - name: Check create / update
        id: checkCreateUpdate
        uses: actions/github-script@v5
        env:
          TAG_LATEST: ${{ steps.previoustag.outputs.tag }}
          TAG_LATEST_DATE: ${{ steps.previoustag.outputs.timestamp }}
        with:
          script: |
            const script = require('./scriptGithub/checkPullRequest.js')
            script({github, context})
      - name: Create Pull Request
        uses: actions/github-script@v5
        env:
          TAG_LATEST: ${{ steps.previoustag.outputs.tag }}
          PROCESS_TYPE: ${{ steps.checkCreateUpdate.outputs.result.processType }}
          PR_NUMBER: ${{ steps.checkCreateUpdate.outputs.result.prNumber }}
        with:
          script: |
            console.log('process.env.PROCESS_TYPE', process.env.PROCESS_TYPE)
            console.log('process.env.PROCESS_TYPE', typeof process.env.PROCESS_TYPE)
            console.log('process.env.PR_NUMBER', process.env.PR_NUMBER)
            console.log('process.env.PR_NUMBER', typeof process.env.PR_NUMBER)
            // const script = require('./scriptGithub/createPullRequest.js')
            // script({github, exec, context})

            // Test
            // fromJSON(steps.checkCreateUpdate.outputs.result.processType) => error reading JToken
            // stringify in script return => not working

      - name: Update Pull Request
        uses: actions/github-script@v5
        env:
          TAG_LATEST: ${{ steps.previoustag.outputs.tag }}
          TAG_LATEST_DATE: ${{ steps.previoustag.outputs.timestamp }}
        with:
          script: |
            const test = ${{ steps.checkCreateUpdate.outputs.result }};
            console.log('test', test);
            // const script = require('./scriptGithub/updatePullRequest.js')
            // script({github, context})
            
